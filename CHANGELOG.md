# 更新日志

## v3.0.0 - 前后端分离与共享组合

- 新增独立 `fund-tracker-v3` 目录，前端从 v2 迁移到 `frontend/` 并保留原有图表、记录、CSV 和主题能力。
- 新增 Spring Boot 3.x + Java 17 后端，使用 Spring Security 6、JWT、Spring Data JPA 和 MySQL 8。
- 新增用户注册、登录、JWT 鉴权和 `/api/auth/me` 当前用户接口。
- 新增基金组合 Portfolio、组合成员 PortfolioMember、基金记录 FundRecord 的持久化模型。
- 新增 OWNER / EDITOR / VIEWER 权限模型，并在后端 Service 层校验组合访问、成员管理和记录编辑权限。
- 前端新增组合列表页和组合详情页，基金记录按指定组合加载、导入、导出和展示。
- 新增 Dockerfile、docker-compose.yml 和 v3 README，用于启动 MySQL 与后端服务。

## v2.4.0 - Remix Icon 与明暗主题

- 接入 Remix Icon 图标样式，替换页面中原有的字符类小图标。
- 顶部导航新增明亮/暗夜模式切换按钮，并放置在用户账户左侧。
- 新增全局主题变量和暗夜模式覆盖样式，主题选择会保存到 LocalStorage。
- 走势分析和收益明细图表会跟随主题切换刷新 tooltip、坐标轴、网格线和图表颜色。

## v2.3.1 - 表单与规则折叠优化

- 新增记录表单调整为更紧凑布局，追加金额与赎回金额同排展示。
- 备注输入框缩小，并将保存记录按钮放在备注右侧。
- CSV 导入规则移入“CSV 导入”卡片内，默认收起，点击后展开。
- 收益计算规则默认收起，点击后展开。
- 走势分析标题图标调整为蓝色趋势图样式。
- 收益计算规则内容调整为单列排布，提升阅读连续性。

## v2.3.0 - 金融数据看板 UI 重构

- 页面整体重构为现代金融后台看板风格，统一浅灰蓝背景、白色卡片、圆角、阴影和蓝色主色。
- 顶部 Header 改为独立大卡片，新增系统图标、产品副标题、用户胶囊信息和更清晰的操作按钮。
- 顶部统计区调整为 5 张核心 KPI 卡片，并将最新交易日涨跌幅改为横向提示条。
- 走势分析和收益明细模块强化卡片层级，优化筛选按钮、图表容器、坐标轴、网格线、tooltip 和图例样式。
- 新增记录表单、历史记录表格和操作按钮进行视觉优化，提升间距、行高、hover、输入 focus 和响应式表现。
- 数据同步、导出 CSV、清空数据、CSV 导入、下载模板和收益计算规则整合为底部“高级功能”区域。
- 保留原有登录、同步、导入导出、计算、编辑删除、图表筛选和本地 mock API 逻辑。

## v2.2.6 - 走势分析 tooltip 点位定位修复

- 修复 v2.2.5 中鼠标移动到对应日期折线位置时 tooltip 不显示的问题。
- 走势分析的业绩走势和累计盈亏均改为点位触发 tooltip，并在每个日期点保留可命中的空心圆点。
- tooltip 位置固定显示在对应折线点附近，内容仍为两行：日期和对应纵坐标值。

## v2.2.5 - 走势分析日期轴悬浮提示

- 走势分析中的业绩走势和累计盈亏都改为按日期轴触发 tooltip。
- 鼠标移动到折线图对应日期位置时，即可显示该日期和对应纵坐标值。
- tooltip 内容改为两行展示：第一行日期，第二行纵坐标值，例如 `2026-3-10` 和 `3.46%`。

## v2.2.4 - 走势分析点位悬浮修复

- 修复 v2.2.3 中业绩走势图主折线无普通点位导致悬浮提示不符合预期的问题。
- 业绩走势和累计盈亏均改为鼠标移动到折线点位置时显示 tooltip。
- tooltip 内容精简为“时间”和“当前纵坐标值”。
- 折线普通点位默认不常驻显示，仅悬浮到点位时出现，避免图表视觉拥挤。

## v2.2.3 - 走势分析悬浮提示

- 走势分析中的业绩走势图和累计盈亏图都增加明确的悬浮提示格式。
- 鼠标移动到折线图对应日期位置时，会自动显示时间和当前纵坐标值。
- 业绩走势图悬浮提示继续保留买入、卖出日期的交易金额信息。

## v2.2.2 - 默认展示范围与区间涨跌幅

- 每次进入首页时，历史记录默认只展示最近 5 条交易日记录，而不是全部记录。
- 历史记录新增“最近5天”快捷按钮，仍保留按年月筛选和查看全部记录。
- 收益明细默认筛选为当前月份，并新增“本月”快捷按钮。
- 业绩走势图在选择近一周、近一月等时间范围后，显示当前区间整体涨跌幅。

## v2.2.1 - 贾涨跌幅展示与模板调整

- 新增记录表单中的输入项更名为“贾-当日涨跌幅”，输入精度保留两位小数。
- 历史记录新增“贾-当日涨跌幅”列，按已保存的计算涨跌幅反推为 2 倍显示。
- 历史记录中的“当日涨跌幅”改为显示参与计算后的涨跌幅，保留 3 位小数。
- CSV 模板表头更新为 `jiaDailyRate`，导入逻辑同时兼容旧模板 `dailyRate`，两者含义均为“贾-当日涨跌幅”。
- CSV 导出新增 `jiaDailyRate` 列，同时保留参与计算后的 `dailyRate` 列。

## v2.2.0 - 版块顺序与涨跌幅计算口径调整

- CSV 导入版块移动到数据管理版块下方。
- 新增记录版块移动到收益明细版块下方。
- 手动输入每日涨跌幅时，表单右侧实时展示参与计算的涨跌幅。
- 后续新增、编辑和 CSV 导入的涨跌幅统一按“输入百分比 ÷ 2”处理，并保留 3 位小数后参与计算。
- 编辑已有记录时会将已保存的计算涨跌幅反推回输入值展示，避免未修改保存时重复折半。
- 更新 CSV 模板和导入规则说明，明确参与计算涨跌幅的折半口径。

## v2.1.1 - 走势分析切换与交易点显示

- 业绩走势和累计盈亏重新合并到同一个“走势分析”区域，通过按钮点击切换显示内容。
- 业绩走势图主折线取消普通日期的空心圆点，仅保留买入、卖出日期的红色和蓝色圆点标记。

## v2.1.0 - 真实模式统一与走势图调整

- 取消页面中的“计算模式”选择，系统统一使用真实模式：当天追加金额不参与当天涨跌。
- 收益计算工具移除简化模式分支，并新增净值字段，用于业绩走势图计算。
- 走势分析恢复为两个独立图表：业绩走势图和累计盈亏图。
- 业绩走势图改为按当前所选区间首日净值计算涨跌百分比，区间首日固定显示为 0%。
- 页面底部新增收益计算规则说明，明确当天追加金额、当天卖出金额、当日收益、当前持仓、累计投入、累计盈亏和累计收益率的计算口径。

## v2.0.1 - CSV 日期格式兼容

- CSV 导入的 `date` 字段新增兼容 `YYYY/M/D` 格式，例如 `2026/3/4`。
- CSV 导入后会将斜杠日期统一规范化为内部 `YYYY-MM-DD`，保证排序、计算和重复日期判断稳定。
- 更新 CSV 模板，示例日期改为 `2026/3/4` 形式。
- 更新 CSV 导入规则说明，明确支持 `YYYY-MM-DD` 和 `YYYY/M/D` 两种日期格式。

## v2.0.0 - Vue3 重构规划与实现

- 将第一版纯 HTML、CSS、JavaScript 本地工具重构为 Vue3 + Vite 项目。
- 引入 Vue Router、Pinia、Axios、ECharts。
- 新增登录、注册、退出登录和路由守卫。
- 新增 CSV 导入、CSV 模板下载、CSV 导出和导入规则说明。
- 预留真实后端 API 层，当前使用 localStorage mock API 保障完整运行。
- 按用户维度隔离基金记录，模拟多设备同步。

## v1.x 第一版迭代记录

- 完成纯前端单页应用，数据保存在浏览器 LocalStorage。
- 顶部概览支持当前持仓、累计投入、累计盈亏、收益率、今日收益、最新涨跌幅。
- 支持新增、编辑、删除基金每日记录。
- 支持业绩走势、每日收益图表和历史记录表格。
- 顶部收益率从累计收益率调整为持有收益率，并在历史记录中增加净值。
- 业绩走势图改为按区间首日净值计算涨跌百分比，区间首日固定为 0%。
- 买入和卖出日期在业绩走势图中分别用红色、蓝色圆点标记。
- 业绩走势图取消平滑曲线，并为纵轴增加上下余量。
- 历史记录支持折叠和按年月筛选。
- 新增累计盈亏折线图。
- “每日收益”重命名为“收益明细”，支持折叠和按月份筛选。
- 业绩走势与累计盈亏合并到同一走势分析区域，通过按钮切换。
- 收益明细移动到新增记录下方。
- 业绩图例改为居中显示，文案为“:买入”和“:卖出”。
